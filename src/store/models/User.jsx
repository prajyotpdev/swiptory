class User {
  constructor(email, name, uid, gender, age, location) {
    this.email = email;
    this.name = name;
    this.uid = uid;
    this.gender = gender;
    this.age = age;
    this.location = location;
  }

  // You can also add methods to the User class if needed
  // For example, a method to format user information, etc.
}

export default User;
